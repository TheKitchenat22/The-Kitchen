/**
 * Live site config — fill jsonbin for GitHub Pages shared admin saves.
 * See config.example.js for setup steps.
 */
window.KITCHEN_CONFIG = {
  jsonbin: {
    binId: "",
    masterKey: "",
  },
  apiBase: "",
};
